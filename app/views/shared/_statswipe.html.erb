<div class="option-swipe" data-action="<%= params[:action] %>">
  <%#raise %>
  <span class="tab moods mood_stats"  ><%= link_to "Mood", mood_stats_path %></span>
  <span class="tab pains pain_stats"><%= link_to "Pain", pain_stats_path %></span>
  <span class="tab sleeps sleep_stats"><%= link_to "Sleep", sleep_stats_path %></span>
  <!-- <span class="tab"><i class="fas fa-chevron-right" id="arrow"></i></span> -->
  <span class="tab exercises exercise_stats"><%= link_to "Exercise", exercise_stats_path %></span>
</div>

<!-- find out the params controller -->
<!--  in js we want to find correct tab class that is same name as controller -->
<!-- params[:controller"] = "moods" <span class="moods"></span> -->
<!-- find span with class of params[:controller] -->
<script>
  const paramsControllerName = document.querySelector('.option-swipe').dataset.action

  const tabs = document.querySelectorAll('.tab');

  tabs.forEach(tab => {
    tab.classList.remove('actives');
    if(tab.classList.contains(paramsControllerName)){
      tab.classList.add('actives')
    }
  })
</script>
